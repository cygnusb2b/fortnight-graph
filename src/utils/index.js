const cleanValues = require('./clean-values');
const isScalar = require('./is-scalar');
const randonBetween = require('./random-between');
const uuid = require('./uuid');

module.exports = {
  cleanValues,
  isScalar,
  randonBetween,
  uuid,
};
